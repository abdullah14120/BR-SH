buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // إصدار مستقر جداً ولا يتطلب تعقيدات Java 21
        classpath 'com.android.tools.build:gradle:7.4.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
